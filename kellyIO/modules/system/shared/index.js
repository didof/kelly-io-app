export const STATUS = {
  IDLE: 0,
  RECORDING: 1,
  SPEAKING: 2,
};
export const INITIAL_STATUS = STATUS.IDLE;

// import LANGS from './LANGS.json'

// export function pickLangs(langs) {
//   if (langs === 'All') return LANGS;

//   return LANGS.filter(([lang]) => langs.includes(lang));
// }


